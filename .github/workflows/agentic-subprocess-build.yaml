name: "Agentic Plugin Build"

on:
  push:
    branches:
      - main
  pull_request:
    paths:
      - "agentic-subprocess/**"
      - ".github/**"
      - "pom.xml"
  workflow_dispatch:

concurrency:
  group: ${{ github.ref == 'refs/heads/main' && format('ci-default-branch-{0}-{1}', github.sha, github.workflow) ||
    format('ci-pr-{0}-{1}', github.ref, github.workflow) }}
  cancel-in-progress: true

jobs:
  build:
    uses: ./.github/workflows/common-build.yaml
    with:
      project-name: "agentic-subprocess"
      project-path: "agentic-subprocess"
    secrets: inherit
